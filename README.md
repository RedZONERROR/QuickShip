# QuickShip
A lightweight, dependency-free deployment toolkit for Linux VPS. Uses pure Bash and Systemd to upload, install, and run standalone executables persistently. Designed to be cloned, used, and completely removed (nuked) after deployment.
